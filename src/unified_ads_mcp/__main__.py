"""Entry point for unified-ads-mcp.

This module allows running the MCP server as a Python module:
    python -m unified_ads_mcp
"""

from unified_ads_mcp.server import main

if __name__ == "__main__":
    main()
